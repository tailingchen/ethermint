package ethereum

import (
	"github.com/tendermint/go-logger"
)

var log = logger.New("module", "ethereum")

/*
func init() {
	log.SetHandler(
		logger.LvlFilterHandler(
			logger.LvlDebug,
			logger.BypassHandler(),
		),
	)
}
*/
